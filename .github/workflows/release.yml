name: Push to release

on:
  release: null
  push:
    branches:
      - release

jobs:
  urbit:
    uses: ./.github/workflows/shared.yml
    with:
      pace: 'soon'
      upload: true
    secrets: inherit
